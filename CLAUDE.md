@AGENTS.md

# CLAUDE.md

## Claude-Specific Notes

- Use `everything-claude-code:go-review` skill when reviewing Go code changes.
- Use `everything-claude-code:go-test` skill when writing or modifying tests.
- Use `everything-claude-code:go-build` skill when fixing build errors.
